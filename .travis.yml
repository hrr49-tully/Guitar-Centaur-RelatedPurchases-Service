arch:
  - amd64

language: node_js

node_js:
    - "stable"

services:
    - "mysql"

install:
    - npm install

addons:
  mariadb: '10.0'

before_install:
  - mariadb -u root < schema.sql;
  - mariadb -u root < db_test.sql;