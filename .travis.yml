arch:
  - amd64

dist: focal

language: node_js

node_js:
    - "stable"

services:
    - "mysql"

install:
    - npm install

addons:
  mariadb: '10.0'

before_install:
  - sudo mysql -u root --password="" < schema.sql;
  - sudo mysql -u root --password="" < db_test.sql;