arch:
  - amd64

language: node_js

node_js:
    - "stable"

services:
    - "mysql"

install:
    - npm install

addons:
  mariadb: '10.0'

before_install:
  - mysql -u root --password="" < schema.sql;
  - mysql -u root --password="" < db_test.sql;